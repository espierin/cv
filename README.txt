ABSTRACT MODAL UPDATE

Replace these repository files:

1. index.html
2. scripts/fetch-orcid.mjs

Then run:
Actions > Refresh ORCID publications > Run workflow

After the workflow and Pages deployment are green, refresh the website with Ctrl+F5.

The updated fetch script adds abstracts, PMID, PMCID and full-text links from Europe PMC where available.
The updated index opens abstracts in an accessible in-page modal.
